<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <insert id="insertBatch">
        insert into setmeal_dish (setmeal_id, name,dish_id,price, copies)
        values
        <foreach collection="setmealDishList" item="setmealDish" separator=",">
            (#{setmealDish.setmealId},#{setmealDish.name},#{setmealDish.dishId},
            #{setmealDish.price},#{setmealDish.copies}
            )
        </foreach>
    </insert>
</mapper>